<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.product.dal.mysql.category.ProductCategoryMapper">
    <select id="getGrandParentId" resultType="java.lang.Long">
        select r.parent_id as grand_parent_id
        from product_category l
                 left join product_category r
                           on l.parent_id = r.id
        where l.id = #{id}
    </select>
</mapper>
